#pragma once

/*
** Linux/bootstrap compatibility shim.
** The active non-Windows bring-up path fences out ImageHlp-dependent code,
** so the header only needs to exist to satisfy legacy includes.
*/
